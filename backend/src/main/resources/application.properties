server.port=${PORT:3000}
server.servlet.context-path=/api

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/surveysite}
spring.datasource.username=${DB_USERNAME:anikin-mm77}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

jwt.secret=${JWT_SECRET:MyVeryLongSecretKeyForJWTTokenGenerationAndValidation123456}
jwt.expiration=${JWT_EXPIRATION:86400000}

allowed.origins=${ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
